name: CybersecFEST — Guardian

on:
  schedule:
    # diariamente às 10h UTC = 07h BRT
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  guardian:
    runs-on: ubuntu-latest
    timeout-minutes: 20
jobs:
  guardian:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: CREAO-PROJECT   # ← adicionar esta linha
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: _scripts/package-lock.json

      - name: Install dependencies
        working-directory: _scripts
        run: npm ci

      - name: Run Guardian
        working-directory: _scripts
        env:
          GH_PAT_CREAO: ${{ secrets.GH_PAT_CREAO }}
        run: node guardian.js
